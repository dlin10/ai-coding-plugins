Return your judgement through the structured output tool.

A command you run takes a `timeout` of up to 1800000 ms (30 minutes); pass it for anything that
may run longer than two minutes, and wait for it in the foreground.
