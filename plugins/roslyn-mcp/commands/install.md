---
description: Install or upgrade the bundled tri-host Roslyn MCP Visual Studio extension (per-solution-port build) into Visual Studio.
allowed-tools: Bash
---

# Install the Roslyn MCP Visual Studio extension

The full procedure lives in the `roslyn-install-vsix` skill — it is the single source of truth for this action.

Read `${CLAUDE_PLUGIN_ROOT}/skills/roslyn-install-vsix/SKILL.md` and follow it exactly, reporting each step.
