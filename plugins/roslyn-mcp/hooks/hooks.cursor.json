{
  "version": 1,
  "hooks": {
    "postToolUse": [
      {
        "command": "powershell -NoProfile -ExecutionPolicy Bypass -File \"${CURSOR_PLUGIN_ROOT}/hooks/roslyn-grep-nudge.ps1\" -HostKind cursor",
        "matcher": "Grep|Shell"
      }
    ]
  }
}
