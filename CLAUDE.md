# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

The guidance lives in @AGENTS.md, which every agent working on this monorepo shares. Keep it there
rather than duplicating it here — a second copy is a second thing to let drift.

Each plugin keeps its own `plugins/<name>/AGENTS.md`; read it before touching that plugin.
